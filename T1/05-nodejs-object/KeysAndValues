const obj = { a: "Espana", b: "Real Betis", c: "WOW" };
function keysAndValues(obj) {
  const keys = Object.keys(obj);
  return [keys, keys.map( key => obj[key] )];
}

console.log(keysAndValues(obj));